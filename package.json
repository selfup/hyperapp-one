{
  "name": "hyperapp-one",
  "version": "2.0.0",
  "description": "Hyperapp One is a Parcel boilerplate for quickstarting a web application with Hyperapp, JSX, Prettier, and Airbnb's JavaScript Styleguide via ESLint.",
  "main": "index.js",
  "scripts": {
    "start": "parcel ./src/index.html",
    "build": "parcel build ./src/index.html --public-url ./",
    "ghp-build": "parcel build ./src/index.html --out-dir build/ --public-url ./build && cp ./build/index.html ./index.html",
    "wipe": "rm -rf .git && git init && git add . && git commit -m 'initial commit'",
    "test": "jest --coverage --no-cache"
  },
  "author": "Regis Boudinot (selfup)",
  "license": "MIT",
  "dependencies": {
    "hyperapp": "^2.0.0"
  },
  "babel": {
    "presets": [
      "@babel/env"
    ],
    "plugins": [
      [
        "@babel/transform-react-jsx",
        {
          "pragma": "h"
        }
      ]
    ]
  },
  "devDependencies": {
    "@babel/core": "^7.5.5",
    "@babel/plugin-transform-react-jsx": "^7.3.0",
    "@babel/preset-env": "^7.5.5",
    "babel-eslint": "^10.0.2",
    "eslint": "^5.16.0",
    "eslint-config-airbnb-base": "^12.0.0",
    "eslint-plugin-import": "^2.18.2",
    "eslint-plugin-jest": "^21.27.2",
    "eslint-plugin-react": "^7.14.3",
    "extract-text-webpack-plugin": "^3.0.0",
    "imports": "^1.0.0",
    "jest": "^24.8.0",
    "node-libs-browser": "^2.2.1",
    "parcel": "^1.12.3"
  }
}
